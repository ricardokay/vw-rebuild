<?php
/**
 * Intentionally empty.
 *
 * comments_template() resolves here (see inc/comments.php). Commenting is off
 * sitewide and the 5,598 existing comments are hidden at the display layer
 * only — nothing is deleted and the decision reverts by removing the require
 * in functions.php.
 */

defined( 'ABSPATH' ) || exit;
